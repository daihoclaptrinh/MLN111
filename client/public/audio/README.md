Put the lottery spin audio file here:

```text
client/public/audio/xoso-mien-bac.mp3
```

The app will play `/audio/xoso-mien-bac.mp3` while spinning an event. If the
file is missing or the browser blocks playback, it falls back to the generated
Web Audio spin sound.
